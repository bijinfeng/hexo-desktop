import { IconFolder } from '@arco-design/web-react/icon';
import React from 'react';

const Folder: React.FC = () => {
  return (
    <div className="flex flex-1 items-center justify-center border-t border-t-border">
      <IconFolder fontSize={72} opacity={0.6} />
    </div>
  );
};

export default Folder;
